{
  "name": "plan-de-classe-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Harnais de tests (dev) pour l'app mono-fichier « plan de classe.html ». Ne fait PAS partie de l'app livrée — aucune dépendance, l'app reste un HTML autonome.",
  "scripts": {
    "test": "node --test"
  }
}
